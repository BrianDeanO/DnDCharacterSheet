import { useEffect, useState } from "react";
import React from "react";

export const SpellsSelectionBox = ({spells}) => {
    return (
        <div className="Notes-MultiSelectBox">
            SPELLS
        </div>
    )
}