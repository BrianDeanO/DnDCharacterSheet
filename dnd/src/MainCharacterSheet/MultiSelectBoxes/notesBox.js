import { useEffect, useState } from "react";
import React from "react";

export const NotesSelectionBox = ({notes}) => {
    return (
        <div className="Notes-MultiSelectBox">
            NOTES
        </div>
    )
}