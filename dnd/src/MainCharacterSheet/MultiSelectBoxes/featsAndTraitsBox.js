import { useEffect, useState } from "react";
import React from "react";

export const FeatsAndTraitsSelectionBox = ({featsAndTraits}) => {
    return (
        <div className="Notes-MultiSelectBox">
            FEATS&TRAITS
        </div>
    )
}