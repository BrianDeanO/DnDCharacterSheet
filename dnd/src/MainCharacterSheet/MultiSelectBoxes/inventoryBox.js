import { useEffect, useState } from "react";
import React from "react";

export const InventorySelectionBox = ({inventory}) => {
    return (
        <div className="Notes-MultiSelectBox">
            INVENTORY
        </div>
    )
}