import { useEffect, useState } from "react";
import React from "react";

export const DecriptionSelectionBox = ({description}) => {
    return (
        <div className="Notes-MultiSelectBox">
            DESCRIPTION
        </div>
    )
}